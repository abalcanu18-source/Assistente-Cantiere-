# Copia questo file in ".env" nella cartella frontend.

# Indirizzo del backend (cambialo con l'IP del PC/server se apri l'app da telefono)
VITE_API_URL=http://localhost:4000

# Stessa chiave pubblica VAPID generata nel backend (npm run generate-vapid)
VITE_VAPID_PUBLIC_KEY=
